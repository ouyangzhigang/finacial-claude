---
description: A股短周期(2周)潜力价值股推荐——按 short-term-stock-picks-prompt-cn.md 规范执行选股全流程并输出报告
argument-hint: [任务参数,如:Top5 2周 稳健偏积极 账户1w]
---

@short-term-stock-picks-prompt-cn.md

## 本次任务参数

$ARGUMENTS

> 若上方为空,使用规范默认输入变量:Top 5 / 短周期 2 周(10 交易日)/ 稳健偏积极 / 账户 1w / 无持仓 / 日期基准=今天 / 数据时效=盘中实时或昨收。

## 执行要求(本机环境已验证,务必遵守)

1. **数据源按规范「本机数据源适配」小节**:ifind 为主力(已实测 `ifind_get_stock_summary` 可取近 1 月每日 OHLCV+涨跌幅+换手率);历史 K 线/短期动量用 `ifind_get_stock_summary`,**不要用** `stock_data.py --history`(走东方财富 push2his,本机经 Whistle/直连均不通);板块用 `ifind_sector_data`;新闻/情绪用 `ifind_search_news`/china-news-mcp;wind 工具需 `WIND_SSL_NO_VERIFY=1`。
2. **选股漏斗纪律**:候选池 ≥30 → 流动性硬门槛过滤 → 七维短线评分排序 → Top N → 组合风控,不得跳过漏斗直接拍脑袋推荐。
3. **短线因子按规范「短线因子定义与计算口径」表算**:短期动量(5/10/20日)、量价突破、资金流连续性、板块 RPS、情绪(连板/封板率/炸板率)、催化兑现度。
4. **评分权重已短线化**:技术25/资金20/催化20/情绪15/基本面10/估值5/流动性5;`quant-factor-screener` 仅用于价值底仓(其动量12-1月、低波动/低换手为中长期口径,别套短线弹性票)。
5. **每只推荐必有**:流动性核验数据 + 数据时效标注(盘中/昨收/延迟15分) + 催化兑现度,空白视为不合格。
6. **多源交叉验证 + 诚实标注**:关键数据至少两源核对,数据缺失处明确标注,不编造。

## 输出要求

- 报告写入 `output/{YYYYMMDD}_短周期2周推荐清单.md`(目录不存在先创建),**不得只在对话中输出文本**。
- 对话回报:文件相对路径 + 一句话结论 + Top N 代码清单 + 总仓位 + 未来 2 周关键催化日历。
- 报告结构按规范「输出要求」节:决策仪表盘 → 宏观与国际形势 → 候选池与流动性过滤 → 评分总表+短线因子明细 → 回测结果 → Top N 操作卡 → 组合风控 → 催化日历 → 风险与情景预演 → 免责声明。

---

现在开始执行:先解析 `$ARGUMENTS` 确认输入变量与日期基准(缺失且无法推断时向用户追问,不臆测),再按规范「执行流程」8 步取数选股,全程数据说话、结论先行、可执行到代码/价位/仓位/止损/目标。
