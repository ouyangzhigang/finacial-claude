# 金融数据工具包 — A股市场依赖
# 安装: pip install -r requirements.txt

# === 核心数据获取 ===
akshare>=1.12.0           # A股数据（无需API密钥）
baostock>=0.9.0           # 稳定服务端数据，历史K线和财务（无需API密钥）
requests>=2.28.0          # 东方财富 push2 API 直连（JSON，非爬虫）

# === 数据处理 ===
pandas>=2.0.0
numpy>=1.24.0

# === 输出 ===
tabulate>=0.9.0           # 表格美化输出
